fusion_depth_calibration_node:
  ros__parameters:
    # Replace with table ROI corners in pixel coordinates (TL, TR, BR, BL).
    roi_corners_2d: [144, 217, 517, 218, 556, 274, 103, 273]
    # Replace with real table ROI size in millimeters [width, height].
    roi_size_mm: [1652, 318]
    num_frames: 30
    safe_zone_margin_ratio: 0.25
    depth_unit_scale: 1.0