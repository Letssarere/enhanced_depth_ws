fusion_depth_calibration_node:
  ros__parameters:
    # Replace with table ROI corners in pixel coordinates (TL, TR, BR, BL).
    roi_corners_2d: [144.0, 217.0, 517.0, 218.0, 556.0, 274.0, 103.0, 273.0]
    # Replace with real table ROI size in meters [width, height].
    roi_size_m: [1.652, 0.318]
    num_frames: 30
    safe_zone_margin_ratio: 0.25
    depth_unit_scale: 0.001
    exit_after_calibration: true
